
<%= form_with(model: task, url:category_tasks_path(category_id:cat_id), :class => 'new-task-form') do |form| %>

<% if task.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(task.errors.count, "error") %> prohibited this entry from being saved:</h2>

      <ul>
        <% task.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div class="">
  <br>

  <div>

    <%= form.text_field :name %>
  </div>

   <div>

    <%= form.text_field :description %>
  </div>


 <div style="display:none">
    <%= form.label :category_id, style: "display: block" %>
    <%= form.text_field :category_id, value: cat_id.id%>
  </div>

  
 <div>
    <%= form.date_field  :deadline, :class => 'deadline' %>
  </div>

<br>

 <div class='submit-container'>
    <%= form.submit "Submit", :class => 'submit-container cursor-pointer border-2 p-2 rounded-lg bg-blue-500 text-white text-sm' %>
  </div>


    <% end %>



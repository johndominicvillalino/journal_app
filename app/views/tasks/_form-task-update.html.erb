
<%= form_with(model: task, url:category_task_url(category_id:cat_id.id, id:task.id),  method: :put , :class => 'edit-form-task flex flex-col gap-5 m-auto items-left form-container container m-6 items-center') do |form| %> 

<% if task.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(task.errors.count, "error") %> prohibited this entry from being saved:</h2>

      <ul>
        <% task.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


 

    <div class="">

  <br>

  <div>

    <%= form.text_field :name, placeholder: 'Task Name'%>
  </div>

   <div>

    <%= form.text_field :description , placeholder: 'Task Description' %>
  </div>


 <div style="display:none">
 
    <%= form.text_field :category_id, value: cat_id.id%>
  </div>

  
 <div class='deadline'>
    <%= form.date_field  :deadline , :class => 'deadline' %>
  </div>

<br>

 <div>
    <%= form.submit "Submit", :class => 'cursor-pointer border-2 p-2 rounded-lg bg-blue-500 text-white text-sm mb-5', :id => 'editTaskBtn' %>
  </div>


    <% end %>


